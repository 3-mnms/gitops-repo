apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: api-gateway
  namespace: argocd
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: gateway
  source:
    repoURL: https://github.com/3-mnms/devops.git
    targetRevision: main
    path: charts/api/charts/gateway
    helm:
      valueFiles:
        - $values-repo/values/api/gateway/values-prod.yaml
        - $values-repo/values/values-prod.yaml
        - $values-repo-secret/tls/values-tls-aws.yaml
  sources: 
    - repoURL: https://github.com/3-mnms/devops.git
      targetRevision: main
      path: charts/api/charts/gateway
      helm:
        valueFiles:
          - $values-repo/values/api/gateway/values-prod.yaml
          - $values-repo/values/values-prod.yaml
          - $values-repo-secret/tls/values-tls-aws.yaml

    - repoURL: https://github.com/3-mnms/gitops-repo.git 
      targetRevision: main
      ref: values-repo
      
    - repoURL: https://github.com/3-mnms/gitops-repo-secret.git 
      targetRevision: main
      ref: values-repo-secret
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true