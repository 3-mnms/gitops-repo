apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: api-booking
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/3-mnms/devops.git  
    targetRevision: main
    path: charts/api/charts/booking  
    helm:
      valueFiles:
        - ../values/api/booking/values-prod.yaml  
        - ../values-prod.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: booking

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true